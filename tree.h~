#include <iostream>
#include <vector>
#include <new>
using namespace std;

struct node{

	int size;
	int depth;
	int key[3];
	node node0;
	node node1;
	node node2;
	node node3;
};

class tree{

	public:
		tree();
		~tree();
		node root;
		
		void insert(int key);
		void search(int key);
};
